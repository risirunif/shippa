build_environment: Ubuntu 14.04
 
before_install:
  - sudo apt-get install -y wget

script: 
  - sudo docker run --rm ubuntu echo "hello world" 

notifications:
  email:
    on_success: change
    on_failure: always 
